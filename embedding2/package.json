{
  "name": "embedding2",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node --enable-source-maps --env-file .env src/index.ts",
    "db:start": "docker compose up -d",
    "db:start:local": "chroma run --path ./chroma_data --port 8000"
  },
  "dependencies": {
    "@chroma-core/default-embed": "^0.1.9",
    "@chroma-core/openai": "^0.1.10",
    "chromadb": "^3.5.0",
    "chromadb-default-embed": "^2.14.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
